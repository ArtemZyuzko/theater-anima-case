# 🎭 Theater Anima — Ticketing & Education Platform

![Project Status](https://img.shields.io/badge/Status-Production-success)
![Python](https://img.shields.io/badge/Python-3.10+-blue?logo=python)
![Flask](https://img.shields.io/badge/Backend-Flask-lightgrey?logo=flask)
![Nginx](https://img.shields.io/badge/Server-Nginx-green?logo=nginx)
![Payments](https://img.shields.io/badge/Integration-Hutko%2FBaas-orange)

**Full-stack веб-додаток для автоматизації продажу квитків та запису на курси акторської майстерності.**

Проект розроблено "під ключ": від налаштування "голого" VPS сервера до інтеграції банківського еквайрингу та SEO-оптимізації.

---

## 📸 Огляд проекту (Screenshots)

| Головна сторінка (Афіша) | Сторінка Школи (Ціни) | Модальне вікно купівлі |
|:---:|:---:|:---:|
| ![Main Page](image_19.png) | ![School Page](image_29.png)
<img width="1723" height="855" alt="Zrzut ekranu 2025-11-29 182741" src="https://github.com/user-attachments/assets/a033853b-48ca-49f4-a9b5-336ec85a17d2" />
<img width="1721" height="855" alt="Zrzut ekranu 2025-11-29 182658" src="https://github.com/user-attachments/assets/98c1f857-f16e-459f-948d-1ff9f207f654" />

---

## 🛠 Технічний стек

### Backend & Infrastructure
* **Python / Flask:** REST API для обробки замовлень та динамічного контенту.
* **Gunicorn + Systemd:** Налаштування WSGI-сервера для роботи у фоновому режимі (daemon).
* **SQLite:** База даних для зберігання замовлень, заявок у школу та контенту афіші.
* **Linux (Ubuntu VPS):** Повне адміністрування сервера, налаштування користувачів, прав доступу (chmod/chown).

### Frontend
* **HTML5 / CSS3:** Кастомний темний дизайн, CSS Grid/Flexbox, адаптивна верстка.
* **JavaScript (ES6+):** Асинхронна взаємодія з бекендом (`fetch`), обробка модальних вікон, валідація форм.
* **TailwindCSS:** Використано для швидкої стилізації окремих компонентів.

### Networking & Security
* **Nginx:** Налаштування Reverse Proxy, редіректи (www/non-www, видалення .html), статичний контент.
* **SSL / HTTPS:** Автоматичне оновлення сертифікатів через **Certbot (Let's Encrypt)**.
* **SEO:** Налаштування `robots.txt`, `sitemap.xml`, OpenGraph мета-тегів.

---

## 💡 Ключові можливості (Features)

### 1. Інтеграція платіжної системи (Hutko API)
Реалізовано безпечний процес оплати через API банку:
* Генерація унікального підпису (Signature) на бекенді для захисту транзакцій.
* Динамічний розрахунок суми залежно від кількості квитків.
* Передача даних клієнта (ім'я, телефон) у банківську виписку для зручності адміністратора.

### 2. Автономність та Відмовостійкість
* Сервер налаштований на **автоматичний перезапуск** у разі збоїв (Systemd service).
* При першому запуску додаток автоматично створює структуру Бази Даних та наповнює її початковим контентом (Seeding).

### 3. "Чисті" URL та UX
* Налаштовано Nginx Rewrite Rules для красивих посилань (наприклад, `/school` замість `/school.html`).
* Lazy Loading (ліниве завантаження) зображень для прискорення роботи сайту.

---

## 💻 Приклад коду (Snippet)

*Оскільки проект є комерційним, повний вихідний код закритий. Нижче наведено приклад реалізації контролера оплати:*

```python
@app.route('/api/pay', methods=['POST'])
def api_pay():
    try:
        data = request.json
        amount_str = str(data.get('amount'))
        
        # 1. Фіксація замовлення в БД перед відправкою в банк
        conn = get_db()
        cur = conn.cursor()
        cur.execute("INSERT INTO orders (description, amount, status) VALUES (?, ?, ?)", 
                   (data.get('description'), amount_str, "pending"))
        conn.commit()
        
        # 2. Формування запиту до платіжного шлюзу
        payment_data = {
            "currency": "UAH",
            "amount": int(float(amount_str) * 100), # Конвертація в копійки
            "order_id": f"order_{int(time.time())}_{cur.lastrowid}",
            "response_url": "[https://theateranima.co.ua](https://theateranima.co.ua)"
        }
        
        # 3. Отримання захищеного посилання (використовує Cloudipsp SDK)
        url = checkout.url(payment_data).get('checkout_url')
        return jsonify({'url': url})

    except Exception as e:
        return jsonify({'error': str(e)}), 500
